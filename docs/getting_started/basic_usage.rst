Basic Usage
===========

.. todo::
   Thread and Process safe usage guide. Composition section makes clear you can't compose atomic instructions.
   Instead, show how to use cmpxchg to create `atomic_mul` function.